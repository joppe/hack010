/*global */

/*global window, Util, Backbone*/

(function (win) {
    'use strict';

    /**
     * @namespace model
     */
    Util.createNamespace('model');

    win.model.State = Backbone.Model.extend({

    });
}(window));